const getters = {
  // common
  count: state => state.common.count,
  currentTab: state => state.common.currentTab,
  menuTabs: state => state.common.menuTabs,
  leftBarMenus: state => state.common.leftBarMenus,
  userInfo: state => state.common.userInfo,
  menu: state => state.common.menu
}

export default getters
